# pyproject.toml — IANUA
# Tooling config for the quality gates defined in AGENTS.md §7-§8.
# Keep this file and .pre-commit-config.yaml in sync with the CI pipeline.

[build-system]
requires = ["setuptools>=84.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = [
  "agents*", "attack*", "intel*", "ingest*", "rag*", "mcp*", "dashboard*", "scripts*",
  "compliance*",
]
exclude = ["tests*"]

# Committed data stores that must ship inside a built wheel — a package
# without its data would degrade silently (e.g. a mapper with no corpus).
[tool.setuptools.package-data]
attack = ["data/*.json", "pins/*.json", "collection-index.json"]
intel = ["*.json", "seed/*.json", "behaviors/*.json", "store.sha256"]
agents = ["policies/policy.json", "mapping/rules/*.json", "mapping/rules.sha256"]
compliance = ["attestations.json"]

[project]
name = "ianua"
version = "2.0.0"
description = "Portfolio-grade environment for AI ops, cybersecurity automation, RAG, and agentic workflows."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Ivan Rozenblad" }]
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]

# Pin runtime deps here as the project grows. Justify each addition per AGENTS.md §4.
dependencies = []

[project.optional-dependencies]
# Install with:  pip install -e ".[dev]"
dev = [
  "ruff==0.16.5",
  "mypy>=2.3.1",
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "hypothesis>=6.165.10",
  "bandit>=1.7",
  "pip-audit>=2.7",
  "detect-secrets>=1.5",
  "pre-commit>=4.6.2",
  "packaging>=26.3",
  "pyyaml>=6.0",
  "reportlab>=5.0.1",
  "cryptography>=50.0.1",
]

dashboard = [
  "streamlit>=1.62.0",
  "qdrant-client>=1.19.0",
  "sentence-transformers>=6.0.0",
]

pdf = [
  "reportlab>=5.0.1",
]

crypto = [
  "cryptography>=50.0.1",
]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["agents", "scripts", "rag", "mcp", "tests"]
extend-exclude = ["data", ".venv", "build", "dist"]

[tool.ruff.lint]
select = [
  "E", "W",
  "F",
  "I",
  "B",
  "S",
  "UP",
  "C4",
  "SIM",
  "PTH",
  "RUF",
]
ignore = [
  "E501",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "S105", "S106"]
# S310: urlopen() target is the module-level loopback constant OLLAMA_URL
# (http://localhost). ruff < 0.16 cannot infer constant string literals, so an
# inline noqa would be flagged as unused (RUF100) on ruff >= 0.16 — a config
# ignore is version-stable in both directions.
"dashboard/ollama_service.py" = ["S310"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
# ruff >= 0.16 formats Python code blocks inside Markdown by default. The
# case-study / doc examples use deliberate comment alignment for readability,
# so keep Markdown out of the formatter (linting is unaffected).
exclude = ["*.md"]

[tool.mypy]
python_version = "3.11"
files = [
  "agents", "attack", "intel", "ingest", "scripts", "tests", "dashboard", "mcp", "rag",
  "compliance",
]
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_return_any = true
no_implicit_optional = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
strict_equality = true

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

[[tool.mypy.overrides]]
module = [
  "qdrant_client",
  "qdrant_client.*",
  "sentence_transformers",
  "streamlit",
  "weasyprint",
  "markdown",
  "yaml",
  "reportlab",
  "reportlab.*",
]
ignore_missing_imports = true


# Legacy exploratory scripts predating the typing baseline; every other
# script is fully type-checked (the previous blanket scripts.* ignore hid
# errors in new gate scripts too).
[[tool.mypy.overrides]]
module = ["scripts.search_cyber_kb_chunked", "scripts.ingest_cyber_kb_chunked"]
ignore_errors = true

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = [
  "-ra",
  "--strict-markers",
  "--strict-config",
  "--cov=agents",
  "--cov=attack",
  "--cov=intel",
  "--cov=ingest",
  "--cov=compliance",
  "--cov=rag",
  "--cov=mcp",
  "--cov-report=term-missing",
  "--cov-fail-under=85",
]
markers = [
  "unit: fast, isolated unit tests",
  "integration: cross-component tests",
  "security: authz / validation / injection / secret-leak tests (must stay green)",
]

[tool.coverage.run]
branch = true
source = ["agents", "attack", "intel", "ingest", "compliance", "rag", "mcp"]
omit = ["tests/*", "*/__init__.py"]

[tool.coverage.report]
show_missing = true
skip_covered = true
exclude_lines = [
  "pragma: no cover",
  "if __name__ == .__main__.:",
  "raise NotImplementedError",
]

[tool.bandit]
exclude_dirs = ["tests", "data", ".venv", "build", "dist"]
