# ci.yml — IANUA
# Place at:  .github/workflows/ci.yml
# Mirrors the fail-fast pipeline in AGENTS.md §8. Local pre-commit == this CI.
#
# Stages (ordered, fail-fast):
#   1. Static analysis  -> ruff check + ruff format --check + mypy
#   2. Security         -> bandit (SAST) + pip-audit (SCA) + SBOM (CycloneDX) + gitleaks (secrets)
#   3. Tests            -> pytest with 85% coverage gate (tests/security must pass)
#   4. Build            -> only on a fully green pipeline
#   5. Deploy           -> MANUAL approval gate (GitHub Environment), never automatic

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Least privilege by default (AGENTS.md §3). Jobs opt into more only if needed.
permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"
jobs:
  # ---------------------------------------------------------------- Stage 1
  static-analysis:
    name: Static analysis (ruff + mypy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
      - name: Verify uv.lock is in sync with pyproject.toml
        # uv.lock is the dependency source of truth; fail closed if pyproject
        # changed without re-locking (and the exported pip locks would drift).
        run: uv lock --check
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install -e ".[dev]"
      - name: Ruff lint
        run: ruff check .
      - name: Ruff format check
        run: ruff format --check .
      - name: Mypy
        run: mypy agents attack intel ingest scripts tests dashboard mcp rag compliance

  # ---------------------------------------------------------------- Stage 2
  security:
    name: Security (bandit + pip-audit + gitleaks)
    runs-on: ubuntu-latest
    needs: static-analysis
    permissions:
      contents: read
      pull-requests: read   # gitleaks-action lists PR commits via the GitHub API
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          fetch-depth: 0          # gitleaks needs full history
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          # 3.12, not the global 3.11: requirements.lock pins the declared
          # [dev,dashboard] closure as resolved on Python 3.12 (e.g. numpy 2.5.0,
          # which requires >=3.12). pip-audit -r dry-run-resolves the lock, so it
          # must run on the interpreter the lock was produced for.
          python-version: "3.12"
          cache: pip
      - name: Install hash-pinned dev toolchain (tamper-evident)
        # The security job's own tools (bandit, pip-audit) install from a
        # SHA-256 hash-pinned lock under --require-hashes, so a tampered or
        # substituted distribution fails the build. Versions match
        # requirements.lock; see security/sbom/requirements-dev.lock.
        run: pip install --require-hashes -r security/sbom/requirements-dev.lock
      - name: Bandit (SAST)
        run: bandit -c pyproject.toml -r agents attack intel ingest scripts mcp
      - name: pip-audit (SCA) — gate on the pinned closure
        # Audit the committed Linux/3.12 lock (declared [dev,dashboard] closure),
        # not the ephemeral CI environment. --disable-pip reads the fully-hashed
        # lock directly with no resolver, so it is platform-independent and
        # deterministic: a non-zero exit means a NEW advisory landed against a
        # pinned dependency -> block the merge.
        run: pip-audit --disable-pip -r security/sbom/requirements.lock
      - name: Generate SBOM (CycloneDX)
        # Regenerate from the same lock for a reproducible SBOM. pip-audit exits
        # non-zero when advisories exist; the SBOM is still written and the gate
        # above already failed the build, so tolerate that exit here — but NOT
        # total tool failure: the checkout's python.cdx.json is deleted first
        # and the output is validated, so a crashed pip-audit (which would
        # silently leave the stale file in place) fails the step instead of
        # producing a stale-but-plausible SBOM. npm half is derived offline
        # from the lockfile.
        run: |
          rm -f security/sbom/python.cdx.json
          pip-audit --disable-pip -r security/sbom/requirements.lock \
            -f cyclonedx-json -o security/sbom/python.cdx.json \
            --progress-spinner off || true
          jq -e '.bomFormat == "CycloneDX"' security/sbom/python.cdx.json >/dev/null \
            || { echo "::error::pip-audit did not produce a usable python.cdx.json"; exit 1; }
          python scripts/generate_sbom.py \
            --timestamp "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
      - name: Upload SBOM artifact
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: sbom-cyclonedx
          path: security/sbom/*.cdx.json
          if-no-files-found: error
      - name: Gitleaks (secret scan)
        uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Pin >= 8.25: .gitleaks.toml's [[allowlists]] + condition syntax is
          # silently IGNORED by older binaries (the action's cache served
          # 8.24.3, un-suppressing the reviewed .secrets.baseline digests and
          # failing the job on lines the allowlist covers). Keep this pin in
          # step with the minimum documented in .gitleaks.toml.
          GITLEAKS_VERSION: "8.30.1"

  # ---------------------------------------------------------------- Stage 3
  tests:
    name: Tests + coverage gate
    runs-on: ubuntu-latest
    needs: security
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: pip install -e ".[dev]"
      - name: Pytest (coverage >= 85%, security suite must pass)
        run: python -m pytest --cov-report=xml
      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml
          if-no-files-found: ignore

  # ------------------------------------------- Stage 3c (real-container sandbox)
  sandbox-enforce:
    name: Sandbox enforcement (Linux container)
    runs-on: ubuntu-latest
    needs: static-analysis
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -e ".[dev]"
      - name: Work around runner podman/crun version skew
        # ubuntu-latest images periodically ship a podman whose generated OCI
        # config is newer than the bundled crun understands — every container
        # start fails with "crun: unknown version specified" (exit 126). Point
        # podman at the runner's maintained runc instead. The `podman info`
        # line verifies the override took effect and fails loudly if runc is
        # unavailable (fail closed, no silent fallback to the broken pair).
        # Remove when the runner image ships a consistent podman+crun again.
        run: |
          mkdir -p ~/.config/containers
          printf '[engine]\nruntime = "runc"\n' > ~/.config/containers/containers.conf
          podman info --format '{{.Host.OCIRuntime.Name}} {{.Host.OCIRuntime.Version}}'
      - name: Pre-pull the sandbox test image
        # The sandbox auto-detects the runtime (podman preferred, docker
        # fallback), and podman has its own image store — pre-pull with podman,
        # not docker, so the tests themselves are fast and network-independent.
        run: podman pull docker.io/library/busybox:stable
      - name: Real-container enforcement tests (enforce mode)
        # Opt-in flag flips the gated negative tests on; a positive control fails
        # loudly if the container cannot start, so the negatives can't pass
        # vacuously. Coverage gate is disabled here (focused subset).
        env:
          MCP_SANDBOX_ENFORCE_TESTS: "1"
        run: >-
          python -m pytest tests/security/test_sandbox.py -q
          -o addopts="" -p no:cacheprovider

  # ---------------------------------------------------------------- Stage 4
  # ------------------------------------------------ Stage 3b (parallel to tests)
  sbom-sync:
    name: SBOM & locks in sync with uv.lock
    runs-on: ubuntu-latest
    needs: static-analysis
    permissions:
      contents: read   # read-only: a drift gate, never auto-commits
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install hash-pinned dev toolchain
        run: pip install --require-hashes -r security/sbom/requirements-dev.lock
      - name: Derived pip locks match uv.lock (source of truth)
        run: python scripts/check_locks.py
      - name: SBOM is regenerated from the committed lock
        # Rebuild the SBOM from the committed lock using the committed timestamp;
        # fail closed if the result differs (i.e. a lock changed but the SBOM was
        # not regenerated). Read-only — the maintainer regenerates, CI never
        # commits. python.cdx.json is intentionally not diffed (pip-audit stamps
        # it with a random serial number); the merged SBOM is deterministic.
        run: |
          ts="$(jq -r .metadata.timestamp security/sbom/sbom.cdx.json)"
          cp security/sbom/sbom.cdx.json /tmp/sbom.before.json
          # Delete the checkout's copy before regenerating: `|| true` tolerates
          # pip-audit's advisories-found exit, but a crashed pip-audit must not
          # silently reuse the stale checkout file and let the diff pass without
          # having verified anything — fail closed on unusable output instead.
          rm -f security/sbom/python.cdx.json
          pip-audit --disable-pip -r security/sbom/requirements.lock \
            -f cyclonedx-json -o security/sbom/python.cdx.json --progress-spinner off || true
          jq -e '.bomFormat == "CycloneDX"' security/sbom/python.cdx.json >/dev/null \
            || { echo "::error::pip-audit did not produce a usable python.cdx.json"; exit 1; }
          python scripts/generate_sbom.py --timestamp "$ts"
          if ! diff -q <(jq -S '.components' /tmp/sbom.before.json) \
                       <(jq -S '.components' security/sbom/sbom.cdx.json) >/dev/null; then
            echo "::error::sbom.cdx.json is stale — regenerate (see security/sbom/README.md)"
            exit 1
          fi
          echo "SBOM components are in sync with the committed lock."

  status-page-sync:
    name: Status page in sync with source data
    runs-on: ubuntu-latest
    needs: static-analysis
    permissions:
      contents: read   # read-only: a drift gate, never auto-commits
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: docs/status.html and status.json match docs/status.data.json
        # Stdlib-only generator; fail closed if the committed outputs drifted
        # from their source data (same pattern as sbom-sync / check_locks).
        run: python scripts/build_status_page.py --check
      - name: docs/trust.html and trust.json match docs/trust.data.json
        # Same drift-gate pattern for the compliance trust page.
        run: python scripts/build_trust_page.py --check
      - name: Install PyYAML for the Sigma-corpus drift gate
        # The status/trust generators are stdlib-only, but the ATT&CK Navigator
        # layer is parsed from Sigma YAML — the one gate here that needs a real
        # YAML parser. Install just PyYAML (small, pure fast wheel) rather than
        # the full dev extra, keeping this drift job lightweight.
        run: pip install "pyyaml>=6.0"
      - name: docs/attack-navigator-layer.json matches the Sigma corpus
        # Same drift-gate pattern: the ATT&CK coverage layer is a pure function
        # of detections/sigma/ (validated against the pinned attack/ corpus),
        # so a new/edited rule must regenerate it.
        run: python scripts/build_attack_navigator.py --check
      - name: ATT&CK corpus integrity gate (pin, shard hashes, tombstones)
        # Verifies the committed attack/ shards against the signed pin without
        # the 54 MB upstream bundle: signature (when present), per-shard
        # SHA-256, canonical rendering, and revocation/successor invariants.
        run: python scripts/update_attack.py --check
      - name: Mapping ruleset digest + validity vs the pinned corpus
        # Canonicalized digest of agents/mapping/rules/ plus a full store load:
        # a rule anchored to a technique the pin marks revoked/deprecated
        # fails here with the successor named — stale mappings cannot merge.
        run: python scripts/check_mapping_rules.py --check
      - name: Intel store digest + validity vs the pinned corpus
        # Same drift-gate pattern for intel/: digest over committed data plus
        # a full fail-closed load (license allow-list, TLP, never-flag,
        # ATT&CK anchors) — poisoned or stale intel content cannot merge.
        run: python scripts/check_intel_store.py --check
      - name: Behavioral index in sync + ATT&CK reference gate
        # The behavioral corpus is authored in YAML and consumed as a
        # committed JSON projection. Rebuilding it here also runs the
        # reference gate: a rule anchored to a revoked or deprecated
        # technique fails with the successor named, so stale post-compromise
        # content cannot merge.
        run: python scripts/build_behavior_index.py --check

  build:
    name: Build package
    runs-on: ubuntu-latest
    needs: tests
    permissions:
      contents: read
      id-token: write       # OIDC for Sigstore keyless signing
      attestations: write   # write the signed SBOM attestation
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install build
      - run: python -m build
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: dist
          path: dist/
      - name: Attest SBOM for the built artifacts (Sigstore, main only)
        # Bind the CycloneDX SBOM to the built distributions with a signed,
        # keyless (OIDC) attestation in the GitHub attestations store. Only on
        # push to main — PRs (incl. forks) lack the id-token to sign, and we
        # only attest artifacts that actually land.
        #
        # Skip condition: the Attestations API returns "Feature not available
        # for user-owned private repositories" for exactly this repo class.
        # We skip (not continue-on-error) so that permission failures, wrong
        # paths, or API errors still break the build in public repos and GHAS
        # orgs where attestation is actually supported.
        #
        # actions/attest-sbom is deprecated; the generic actions/attest is its
        # documented replacement. It no longer infers the SBOM format, so the
        # CycloneDX in-toto predicate type is stated explicitly.
        if: >-
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          !(github.event.repository.private == true &&
            github.event.repository.owner.type == 'User')
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: "dist/*"
          predicate-type: "https://cyclonedx.org/bom"
          predicate-path: "security/sbom/sbom.cdx.json"
      - name: Attest build provenance for the artifacts (SLSA, main only)
        # SLSA build-provenance attestation for the distributions: records the
        # source commit, builder identity (GitHub Actions), and materials, so a
        # consumer can confirm *what was built, from where, by which workflow*.
        # Complements the SBOM attestation above. Same skip guard: keyless OIDC
        # signing, push-to-main only, and skipped on user-owned private repos
        # where the Attestations API is unavailable — a fail-closed guard, not
        # continue-on-error, so real signing errors still break the build.
        if: >-
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          !(github.event.repository.private == true &&
            github.event.repository.owner.type == 'User')
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4
        with:
          subject-path: "dist/*"

  # ---------------------------------------------------------------- Stage 5
  deploy:
    name: Deploy (manual approval gate)
    runs-on: ubuntu-latest
    needs: build
    # Only ever runs on an explicit, manual dispatch from main — never on PRs.
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
    environment:
      name: production        # configure required reviewers in repo Settings > Environments
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: dist
          path: dist/
      - name: Deploy placeholder
        run: |
          echo "Human approval recorded via the 'production' environment gate (AGENTS.md §5.1)."
          echo "Wire the real deploy step here. Keep secrets in the env's secret store only."
